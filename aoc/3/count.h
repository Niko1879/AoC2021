#pragma once
#include <string>
#include <vector>


std::vector<std::string> Transpose(const std::vector<std::string>& strs);

char MostCommonBit(const std::string& binaryValue);

std::string Reduce(const std::vector<std::string>& values, bool lowest = false);

